chunking:
  paragraph_split_threshold_chars: 700
  dedupe_repealed_ranges: true

embedding:
  model_name: "BAAI/bge-m3"
  batch_size: 32

qdrant:
  collection_name: "egyptian_civil_code"
  storage_path: "data/processed/qdrant_storage"
